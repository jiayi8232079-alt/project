---
name: alipay-pay
description: 支付宝 Alipay 支付实战排障版技能，覆盖 Alipay SDK、RSA2、应用公钥证书/支付宝公钥证书、APP/H5/PC/小程序支付、trade_no/out_trade_no、异步通知验签、退款、分账、资金流水对账、幂等补偿、沙箱生产差异、2024-2026 证书与接口变化。涉及支付宝支付接入、改造、排障、回归、发布、证书或账务闭环时必须触发。
alwaysApply: false
---

# 支付宝支付实战排障版

定位：只处理支付宝支付链路的工程排障、证据链、领域坑点、验证口径与交付检查；不替代语言 SDK、数据库、API、发布平台或安全审计技能，不做真实支付调用，不索要密钥，不提供绕过风控、欺诈交易、套现或规避合规的方案。

## 快速总则

- 先定入口：下单、同步返回、异步通知、查单、退款、关闭、撤销、分账、转账、账单、对账、证书轮换、ISV 代调用、支付宝小程序支付或发布回滚；入口不清先不改。
- 先定版本：通用版开放平台接口、v3 接口、官方 SDK 版本、语言运行时、TLS/编码/时区、普通公钥模式或公钥证书模式；禁止把微信 v3、分整数、JSON 回调规则套给支付宝。
- 先定环境：沙箱与生产必须成套隔离 app_id、商户号/PID、网关、应用私钥、应用公钥、支付宝公钥、应用公钥证书、支付宝公钥证书、支付宝根证书、AES 密钥、notify_url、return_url、app_auth_token 和日志级别。
- 先定证据：请求参数摘要、支付宝响应 code/sub_code、trade_no、out_trade_no、buyer_id 脱敏值、异步通知原始 form、验签材料、订单/退款/分账/账单状态、trace_id、SDK 版本、配置来源和发布时间；未读证据不下结论。
- 支付成功口径：同步返回只做展示或触发查单，最终以异步通知验签通过且业务字段校验通过，或主动查单确认成功为准。
- 回调底线：异步通知必须按 POST form 原参验签，校验 app_id、seller_id/商户号、out_trade_no、trade_no、total_amount、状态和幂等锁，成功后返回纯文本 success。
- 金额底线：支付宝 total_amount/refund_amount 使用元字符串，禁止 float，所有优惠、手续费、退款、分账和跨日差异进入资金流水对账。
- 收口底线：不能只等回调；必须有查单、退款查询、分账查询、账单下载、逐笔对账、超时重试、补偿任务、告警和人工处理出口。

## 场景执行卡

### 1. 新接订单支付：APP / H5 / PC / 当面付 / 支付宝小程序
- 先查：产品类型、客户端入口、直连或 ISV、通用版或 v3、SDK 版本、签名模式、网关、app_id、商户号/PID、订单号规则、金额来源、notify_url 可达性。
- 动作：生成全局唯一 out_trade_no；total_amount 使用元字符串；服务端签名；APP 返回 orderStr，H5/PC 返回跳转或表单，当面付返回二维码，小程序走对应 my.tradePay 或插件/服务端产品要求。
- 证据：保存请求摘要、响应 code/sub_code/msg、trade_no/out_trade_no、金额、客户端拉起结果、同步返回参数、查单结果、trace_id 和脱敏配置快照。
- 验证：成功、取消、超时、重复点击、弱网、金额边界、客户端版本差异、沙箱到生产切换、notify_url 域名和 TLS。

### 2. 异步通知 / 同步返回 / 查单补偿
- 先查：通知是否 POST form，路由是否透传原始参数，中间件是否改写 body/编码，验签用支付宝公钥还是支付宝公钥证书，success 响应是否纯文本。
- 动作：同步返回不改最终成功；异步通知先验签再做幂等锁和业务字段校验；通知丢失、超时或验签异常时按 out_trade_no 主动查单补偿。
- 证据：原始通知参数摘要、sign/sign_type、验签结果、证书序列号、幂等命中、状态迁移、响应文本、支付宝重试次数、查单补偿结果。
- 验证：重复通知、乱序通知、伪造签名、金额不符、订单不存在、处理超时、通知丢失、查单成功但回调未到。

### 3. 签名 / 验签 / RSA2 / 公钥证书 / 轮换
- 先查：sign_type 是否 RSA2，普通公钥模式还是证书模式，应用私钥、应用公钥、支付宝公钥、应用公钥证书、支付宝公钥证书、支付宝根证书是否成套匹配。
- 动作：私钥只在服务端安全配置；证书模式保留 cert_sn、alipay_root_cert_sn、支付宝公钥证书序列号；轮换必须灰度、双读或快速回滚；沙箱与生产证书完全隔离。
- 证据：签名原串摘要、验签参数集合、证书序列号、证书有效期、SDK 版本、配置来源、失败 sub_code、容器镜像/环境变量版本。
- 验证：下单签名、回调验签、证书过期、证书不匹配、根证书漏配、网关切换、编码/换行/时区差异、SDK 升级兼容。

### 4. 退款 / 关闭 / 撤销
- 先查：订单状态、可退金额、已退金额、退款号 out_request_no、关闭 close 与撤销 cancel 边界、退款查询入口、退款资金流入账规则。
- 动作：未支付关闭用 close；处理中按产品语义撤销；已支付退款用 refund；每个退款意图有稳定 out_request_no，部分退款每笔独立幂等号；失败先查退款状态再重试。
- 证据：退款请求、支付宝退款响应、refund_detail_item_list、退款查询、原订单状态、剩余可退金额、账务记录和对账差异。
- 验证：全额退款、部分退款、多次部分退款、重复提交、超退、退款处理中、退款失败补偿、退款到账与账单差异。

### 5. 分账 / 转账 / 结算 / 资金流水对账
- 先查：产品签约、分账关系、转账权限、公钥证书要求、out_biz_no/out_request_no、接收方身份、结算周期、账单下载权限和账单类型。
- 动作：分账前校验可分账金额和接收方；转账/资金类接口按要求使用公钥证书；账单下载后逐笔核对订单、退款、手续费、分账、跨日交易和补差。
- 证据：分账/转账请求、支付宝单号、状态查询、账单文件摘要、差异列表、补偿任务、人工处理记录、风控或权限 sub_code。
- 验证：幂等重试、金额边界、接收方异常、账单缺行、账单延迟、跨日退款、手续费差异、分账回退。

### 6. ISV 服务商 / 授权 / 多商户
- 先查：平台 app_id、商户 app_id/PID、app_auth_token、授权有效期、商户配置隔离、回调归属、账单归属和审计要求。
- 动作：代调用必须传 app_auth_token；按商户隔离订单号、证书、账单、通知处理、幂等锁和审计日志；授权失效走重新授权、暂停交易或人工处理。
- 证据：授权记录、代调用请求摘要、商户维度订单、通知验签、权限错误、账单归属、审计日志。
- 验证：单商户、多商户并发、授权失效、商户切换、错商户回调、错账单归属、配置串线。

### 7. 超时 / 风控 / 发布 / 回滚
- 先查：网关超时、SDK 超时、重试次数、幂等键、风控 sub_code、生产开关、回调域名、证书发布窗口、灰度策略和告警规则。
- 动作：外部请求设置超时；网络失败先查单再决定重试；风控错误只给合规文案；证书/网关/notify_url 变更必须灰度、小金额验证、可回滚。
- 证据：超时日志、请求耗时、重试次数、查单结果、风控 sub_code、发布批次、告警、回滚验证和小金额交易单。
- 验证：网关慢、SDK 异常、重复请求、风控拒绝、证书回滚、notify_url 切换、告警触发、补偿任务恢复。

## 高频坑 / 防遗漏

### 高频坑
- 金额按分传、float 计算、字符串精度丢失，导致金额不符或验签后的业务校验失败。
- 异步通知当 JSON 解析，或中间件重排/过滤参数，导致验签材料变化。
- 同步返回直接改为支付成功，跳过异步通知、查单和账务对账。
- 回调只验签不校验 app_id、seller_id、out_trade_no、trade_no、total_amount 和订单状态。
- 返回 JSON、HTML 或带引号的 success，导致支付宝继续重试。
- 应用公钥、支付宝公钥、应用公钥证书、支付宝公钥证书、根证书混淆。
- 沙箱和生产网关、app_id、证书、私钥、notify_url、app_auth_token 串线。
- 通用版与 v3 SDK、接口字段、证书加载方式、错误码处理混用。
- 退款、分账、转账共用订单号或幂等号，导致重复扣退或无法追账。
- 日志打印私钥、完整证书、buyer_id、手机号、token、完整通知原文或未脱敏账单。

### 防遗漏清单
- 改下单：查产品、金额元字符串、唯一订单、notify_url、客户端入口、SDK/平台版本、沙箱生产配置。
- 改回调：查 form 原参、验签、公钥/证书、幂等、金额与商户校验、纯文本 success、补偿查单。
- 改退款：查可退金额、out_request_no、退款查询、重复重试、账务状态、退款账单。
- 改分账/转账：查签约、证书模式、接收方、幂等号、状态查询、风控、账单对账。
- 改证书：查 cert_sn、alipay_root_cert_sn、有效期、环境隔离、灰度、回滚和告警。
- 改小程序：查基础库、my.tradePay 参数、授权链路、服务端 orderStr/交易号、端内错误码和审核限制。
- 改发布：查网关、域名、TLS、容器时区、环境变量、灰度、小金额验证、回滚配置。

## 输出要求

涉及支付宝支付时输出必须可复核：
1. 场景卡：说明命中的入口、平台和交易阶段。
2. 版本环境：通用版/v3、SDK、语言运行环境、沙箱/生产、直连/ISV、普通公钥/公钥证书。
3. 关键证据：请求、响应、异步通知、同步返回、验签、订单/退款/分账/账单状态、脱敏日志位置。
4. 风险判断：金额、签名、验签、证书、公私钥、app_id、商户号、网关、幂等、超时、重试、风控、合规、日志脱敏。
5. 验证路径：沙箱用例、生产小金额、异常回调、主动查单、退款/分账查询、账单逐笔对账、告警和补偿。
6. 联动技能：说明是否需要 api-design、db-design、web-security、backend-engineering、test-engineering、code-audit、release-engineering、observability-sre、alipay-miniprogram 或语言技能。

## 约束

- 只维护支付宝支付排障技能，不替代具体语言 SDK 实现、API 路由设计、数据库迁移、发布脚本、SRE 监控或测试框架。
- 不请求、不展示、不记录真实应用私钥、支付宝公钥证书内容、商户号敏感配置、AES 密钥、app_auth_token、用户支付隐私或完整账单明细。
- 不提供绕过支付宝风控、伪造回调、欺诈交易、规避合规审核、洗钱、套现或批量试探风控的方案。
- 支付宝开放平台、商户后台、官方 SDK、线上脱敏证据和资金账单是最终权威；不凭记忆断言新接口行为。
- 未确认调用方、消费方、状态机、数据表、配置来源、回调路由、补偿任务、账单任务、告警和发布开关前，不进入实现修改。
- 所有日志必须脱敏，但保留 trace_id、out_trade_no、trade_no、sub_code、环境、版本、证书序列号等排障所需字段。

## 高频 Bug 反例库

- 反例 1：错法：把支付宝 total_amount 当微信分整数传。对法：使用元字符串并校验小数精度、币种和订单金额。根因：混淆支付宝与微信支付金额单位，且缺少金额业务校验。
- 反例 2：错法：异步通知按 JSON body 解析后再验签。对法：按支付宝 POST form 原始参数集合验签，排除 sign/sign_type 后使用 SDK 规则。根因：中间件改变验签材料。
- 反例 3：错法：用户从同步返回页回来就把订单置为成功。对法：同步返回只展示或触发查单，最终以异步通知或主动查单确认。根因：混淆前端跳转成功与资金成功。
- 反例 4：错法：回调验签通过后不校验 app_id、seller_id、out_trade_no、trade_no、total_amount。对法：验签后继续校验业务归属、金额和状态机。根因：把签名真实性误当业务正确性。
- 反例 5：错法：回调处理成功返回 JSON 或 success 带引号。对法：返回纯文本 success，失败时让支付宝按规则重试。根因：忽略支付宝通知响应协议。
- 反例 6：错法：退款失败后更换 out_request_no 盲目重试。对法：同一退款意图保持幂等号，先查退款状态再补偿。根因：把网络失败等同业务失败，造成重复退款风险。
- 反例 7：错法：多次部分退款复用同一个 out_request_no。对法：每笔部分退款使用独立幂等号并记录剩余可退金额。根因：未区分退款意图与原支付订单。
- 反例 8：错法：把应用公钥配置成支付宝公钥，或只上传公钥不更新证书链。对法：区分应用私钥、应用公钥、支付宝公钥、应用公钥证书、支付宝公钥证书和根证书。根因：证书角色和信任方向混淆。
- 反例 9：错法：沙箱 app_id 配生产网关、生产证书或生产 notify_url。对法：按环境成套隔离配置，上线前生产小金额验证。根因：配置缺少环境维度和发布校验。
- 反例 10：错法：ISV 代调用漏传 app_auth_token 或复用其他商户授权。对法：平台身份与商户授权同时校验，授权失效进入重新授权流程。根因：多商户隔离和授权状态管理缺失。
- 反例 11：错法：转账、资金类接口仍用普通公钥模式。对法：按接口要求使用公钥证书模式并校验证书序列号。根因：以为下单能通代表全部资金接口能通。
- 反例 12：错法：日志打印完整私钥、证书、buyer_id、手机号、token 或账单明细。对法：日志脱敏，仅保留 trace_id、out_trade_no、trade_no、sub_code、版本和环境。根因：排障证据与敏感数据边界不清。
- 反例 13：错法：网络超时后直接再次下单生成新 out_trade_no。对法：先按原 out_trade_no 查单，再按幂等策略重试或关闭旧单。根因：外部超时状态未知却创建新交易。
- 反例 14：错法：账单对账只看总额，不逐笔核对退款、手续费、分账和跨日交易。对法：下载资金账单后逐笔核对并把差异进入补偿队列。根因：把通知成功误当账务闭环。
- 反例 15：错法：证书轮换只替换应用证书，遗漏支付宝公钥证书或根证书。对法：证书链成套更新，记录 cert_sn/alipay_root_cert_sn 并保留回滚。根因：不了解证书模式的链式校验。
- 反例 16：错法：支付宝小程序端直接拼支付参数或暴露私钥。对法：服务端生成交易与签名，小程序只接收受控参数并调用端能力。根因：把客户端调起能力误当服务端支付能力。

## 提交前自检清单

- [ ] 已确认远端 raw 为唯一权威源，未读取或修改本地 skills 文件、本地 SQLite、data/skills.db。
- [ ] frontmatter 含 name 和 description，H1 为“支付宝支付实战排障版”。
- [ ] 行数小于 500，fenced code block 数量为 0，正文不出现代码围栏。
- [ ] 必需章节齐全：快速总则、场景执行卡、高频坑 / 防遗漏、输出要求、约束、高频 Bug 反例库、提交前自检清单、2024-2026 新坑速查、与相邻技能的边界。
- [ ] 反例数量不少于 10，且每条能被“反例 数字”命中，并包含错法、对法、根因。
- [ ] 支付宝、Alipay、Alipay SDK、支付、APP、H5、PC、小程序、订单、退款、回调、异步通知、同步返回、签名、验签、RSA2、应用公钥证书、支付宝公钥证书、公钥、私钥、app_id、商户号、网关、沙箱、生产、trade_no、out_trade_no、幂等、金额、分账、账单、对账、资金流水、超时、重试、风控、合规、日志脱敏均无缺失。
- [ ] 已覆盖版本、平台、SDK、运行环境差异，以及入口、复现、证据、验证路径。
- [ ] 已覆盖安全、权限、数据、兼容、发布、回滚、观测和告警风险。
- [ ] 已明确与 api-design、backend-engineering、web-security、db-design、alipay-miniprogram、release-engineering、observability-sre、test-engineering、code-audit、wechat-pay 和语言技能的边界。

## 2024-2026 新坑速查

- 2024-2026 通用版与 v3 并存：同一项目可能因接口覆盖、SDK 版本、签名材料、证书加载方式不同出现差异；先确认目标接口和 SDK 版本再改。
- 2024-2026 公钥证书模式扩大：转账、资金类能力、部分商户能力更常要求应用公钥证书、支付宝公钥证书和支付宝根证书；普通公钥能下单不代表资金接口能通。
- 2024-2026 支付宝小程序支付变化：基础库、端能力、插件、审核和 my.tradePay 参数差异会影响调起；端内失败必须联动服务端查单。
- 2024-2026 合规提示更严：自动续费、周期扣款、花呗分期、营销补贴、分账和退款规则需检查用户授权、页面提示、协议留痕和可取消路径。
- 2024-2026 风控拦截常态化：不要把 sub_code 原样暴露给用户；按合规文案、人工审核、重试间隔、证据留存和客服处理闭环。
- 2024-2026 多端平台差异：APP、H5、PC、小程序、服务端 SDK 在调起参数、编码、证书加载、TLS、时区和容器证书路径上可能不同。
- 2024-2026 账单与补偿要求提高：通知成功不等于资金闭环，退款、分账、手续费、跨日交易、账期延迟必须进入逐笔对账。
- 2024-2026 发布风险更集中：证书过期、根证书变更、网关切换、notify_url 域名、容器时区、依赖升级、WAF 拦截都可能让线上验签或回调失败。
- 2024-2026 SDK 版本坑：旧 SDK 对 v3、证书序列号、TLS 或错误码支持不足；升级 SDK 必须回归签名、验签、退款、分账和账单下载。
- 2024-2026 证据合规冲突：排障需要原始参数摘要和 trace_id，但不能保存完整私钥、证书、token、buyer_id、手机号或未脱敏账单。

## 与相邻技能的边界

- api-design：负责支付 API 路由、响应结构、认证、错误码、回调暴露面和幂等接口契约；本技能负责支付宝字段、签名、通知语义和交易闭环。
- backend-engineering：负责服务端架构、任务队列、事务边界、配置加载、并发控制和运行时问题；本技能提供支付宝支付专项检查点。
- web-security：负责鉴权、CSRF、SSRF、回调暴露面、密钥管理、日志脱敏和攻击面审计；本技能只定义支付宝验签、敏感字段和风控边界。
- db-design：负责订单、退款、分账、转账、账单表、唯一索引、事务和状态机；本技能负责 trade_no/out_trade_no/out_request_no/out_biz_no 等支付宝幂等号和资金字段约束。
- alipay-miniprogram：负责 AXML/ACSS、my.* API、基础库、端内授权、审核和小程序端支付调起；本技能负责服务端交易、签名、回调和账务闭环。
- release-engineering：负责证书、网关、notify_url、环境变量、灰度、小金额验证、回滚、发布窗口和生产开关；本技能定义支付宝发布风险样本。
- observability-sre：负责指标、日志、trace、告警、SLO、补偿任务观测和事故复盘；本技能定义支付链路必须保留的证据字段和告警触发点。
- test-engineering：负责复现、沙箱、异常通知、回归矩阵、自动化和发布冒烟；本技能提供支付风险样本与验收口径。
- code-audit：代码改动完成前最终收口，查影响面、证据、状态机、安全和遗漏；本技能提供支付宝专项审计点。
- research：只在需要查支付宝开放平台最新政策、接口变更、SDK 发布说明和 2024-2026 外部资料时使用；本技能负责把资料落到排障口径。
- wechat-pay：只负责微信支付；不要把微信的分整数、JSON v3、平台证书、回调语义套到支付宝。
- 语言技能：go-dev、java-dev、php-dev、python-dev、js-ts-dev 等负责具体 SDK 代码；本技能不替代语言实现。
