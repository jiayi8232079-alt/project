#!/usr/bin/env python3
"""Assemble PCB8 check report v2 (6-layer + USB detail)."""
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
USB = ROOT / "_usb_detail_section.md"
OUT = ROOT / "PCB8-设计检查报告.md"

HEADER = """# PCB8 设计检查报告（只读）

> **工程**：jiajia-2026年6月4日  
> **检查对象**：PCB8（主板，**六层板**）及关联原理图 Schematic1 / P12  
> **叠层（自上而下）**：L1 器件 → L2 GND → L3 SIG → L4 PWR → L5 GND → L6 器件  
> **检查方式**：嘉立创EDA API 只读检查（未修改任何设计文件）  
> **检查日期**：2026-06-06（v2：六层叠层 + USB 逐段明细）

---

## 1. 检查范围说明

| 项目 | 内容 |
|------|------|
| 主板 PCB | **PCB8**（**6 层**，成品板厚建议 **1.6 mm**） |
| 关联原理图 | **Schematic1 / P12** |
| 未纳入本报告 | PCB9（空板）、PCB10（副板，未布线） |

> 设计指导文档仍写 PCB7/四层；当前工程主板已迭代为 **PCB8 六层**。打样与归档请以 **PCB8** 为准。

---

## 2. 六层叠层定义与 EDA 层号映射

| 物理层 | 功能 | 嘉立创EDA 层号 | 走线统计（线段数） |
|--------|------|----------------|-------------------|
| **L1** | 顶层 · 器件 | `1` (TOP) | **1324** |
| **L2** | 内层1 · **GND** | `15` (INNER_1) | 0（应敷铜，非走线层） |
| **L3** | 内层2 · **SIG** | `16` (INNER_2) | **175**（含 USB_VBUS 7 mil） |
| **L4** | 内层3 · **PWR** | `17` (INNER_3) | 0（应敷铜/电源） |
| **L5** | 内层4 · **GND** | `18` (INNER_4) | 0（应敷铜） |
| **L6** | 底层 · 器件 | `2` (BOTTOM) | **1292** |

> **说明**：此前按四层理解时，曾将 EDA 层号 `2` 误当作内层；在六层板中 **`2` = 底层（L6）**，`16` = 内层 SIG（L3）。

### 2.1 检查结论摘要

| 维度 | 结论 | 风险 |
|------|------|------|
| PCB DRC | **通过**（0 错误） | 低 |
| 原理图 DRC | **通过**（17 条警告） | 中 |
| 六层叠层实践 | L2/L4/L5 **无敷铜记录**；L3 走信号含 VBUS | **高** |
| 布局 | 59% 器件在 L6 底层 | **高** |
| USB 差分 5.7 mil | L1 估算 **97.5 Ω**（略高）；L6 估算 **104.2 Ω**（偏高） | **中** |
| USB_VBUS | 7 mil 走在 **L3(SIG)** 而非 L4(PWR) | **高** |
| 原理图电气整洁度 | 孤立线 30、零长线 36、无网名线 278 | **高** |

**总体建议**：PCB8 已完成相当规模的布局布线，DRC 无硬错误，但六层叠层用法、底层大量走线、USB 底层阻抗偏高、VBUS 走 SIG 内层等问题，**暂不建议直接 Gerber 量产**。

---

## 3. 原理图 Schematic1 / P12

### 3.1 DRC

| 项目 | 结果 |
|------|------|
| DRC 状态 | 通过（`drcPass: true`） |
| 警告数量 | **17**（请在 EDA 内打开 DRC 面板逐项查看） |

### 3.2 规模统计

| 项目 | 数量 |
|------|------|
| 器件 | 479 |
| 导线 | 491 |
| 重复位号 | 0 |
| 无位号器件 | 204 |

### 3.3 导线问题统计

| 类型 | 数量 | 优先级 |
|------|------|--------|
| 完全孤立导线 | **30** | P0 |
| 零长度导线 | **36** | P0 |
| 无网名 `(no-net)` 导线 | **278** | P1 |
| 单端开口导线 | **271** | P1 |

### 3.4 孤立导线涉及的关键网络（示例）

`PWR_KEY`、`CH_DTR0` / `CH_RTS0`、`I2C_SYS_SDA` / `I2C_SYS_SCL`、`CIS_SDA` / `CIS_SCL`、`3V3_T5`

---

## 4. PCB8 布局

| 项目 | 数量 | 占比 |
|------|------|------|
| 器件总数 | **275** | 100% |
| L1 顶层 | 114 | 41% |
| L6 底层 | **161** | **59%** |

底层重要器件：`U1`, `U4`, `U5`, `U8`, `U15`, `U17`, `U32`, `U35`, `U50`, `CN12`, `CN20`

板框焊盘：API 无法解析 U 形板框多边形，请在本机 EDA 使用 **DFM 预览**人工检查。

---

## 5. PCB8 布线

| 项目 | 数量 |
|------|------|
| 走线 | **2791** |
| 过孔 | **681** |
| 已布线网络 | **188** |
| 敷铜 Fill/Region（API） | **0** |
| DRC 错误 | **0** |

### 5.1 已配置的差分对（共 14 组，节选 USB 相关）

| 名称 | 正网络 | 负网络 |
|------|--------|--------|
| USB_D | USB_DP | USB_DN |
| U_D | U_DP | U_DN |
| DP1 | USB_4G_DP | USB_4G_DM |

---

## 6. USB 走线三种线宽 — 总览

| 线宽 (mil) | 用途 | 线段数 | 总长度 (mil) | 总长度 (mm) | 主要所在层 |
|------------|------|--------|--------------|-------------|------------|
| **5.7** | 差分：USB_DP/DN、U_DP/DN、USB_4G_DP/DM | 107 | **5812.2** | 147.6 | L1 为主，部分 L6 |
| **7.0** | 电源：USB_VBUS | 8 | **410.9** | 10.4 | **L3(SIG) 349 mil**，L6 62 mil |
| **5.0** | 电源：USB_VBUS | 2 | **48.9** | 1.2 | L1 |

差分 **线隙（边到边）约 7.0 mil**（中心距 **12.7 mil** − 线宽 **5.7 mil**）。

### 6.1 按网络汇总（5.7 mil 差分）

| 网络 | 段数 | 总长(mil) | 总长(mm) | L1 | L6 |
|------|------|-----------|----------|-----|-----|
| USB_4G_DM | 14 | 2191.9 | 55.7 | 2136 | 56 |
| USB_4G_DP | 14 | 2163.9 | 55.0 | 2107 | 57 |
| USB_DN | 53 | 655.8 | 16.7 | 407 | 248 |
| USB_DP | 20 | 691.6 | 17.6 | 447 | 244 |
| U_DN | 3 | 55.5 | 1.4 | 0 | 55 |
| U_DP | 3 | 53.5 | 1.4 | 0 | 54 |

"""

FOOTER = """
---

## 8. 与六层叠层设计目标对照

| 项目 | 设计目标 | PCB8 现状 | 达标 |
|------|----------|-----------|------|
| L2 GND | 完整地敷铜 | API 未见敷铜；无走线 | ❌ |
| L3 SIG | 一般信号 | 175 段走线，**含 USB_VBUS** | ⚠️ |
| L4 PWR | 电源分区敷铜 | 无走线/无敷铜记录 | ❌ |
| L5 GND | 完整地敷铜 | 无敷铜记录 | ❌ |
| USB 差分 90 Ω | L1/L6 关键路径 | L1≈97.5 Ω；**L6≈104.2 Ω** | ⚠️ |
| USB 少换层 | 顶层优先 | 4G 主路径 L1；USB 座段在 **L6** | ⚠️ |
| 连接器/主控 | 顶层朝出线侧 | 多颗在 L6 底层 | ❌ |

---

## 9. 打样前建议（更新）

### P0 — 必须

1. 用嘉立创阻抗神器按 **六层 1.6 mm** 选定叠层编号，复核 **5.7/7.0 mil** 线宽线距。  
2. **USB_VBUS** 从 L3(SIG) 迁至 **L4(PWR)** 或 L1 铺铜。  
3. 缩短/避免 **L6 底层** USB_DP/DN、U_DP/DN（当前 Zdiff≈104 Ω，超标约 +14%）。  
4. L2/L4/L5 补 **敷铜**；原理图清理孤立线。  
5. `U1` / `U32` / `CN*` 等尽量移回 L1 顶层。

### P1 — 强烈建议

6. 展开本报告「5.7 mil 逐段明细」核对最长段：USB_4G 约 **1018 mil** 水平段、USB 座附近 D2/D3/R3/R4。  
7. 嘉立创 **DFM 预览** + 17 条原理图 DRC 警告全文。  
8. 将设计指导文档 **PCB7 四层** 更新为 **PCB8 六层**（避免返单混淆）。

---

## 10. 数据文件与检查方法

| 文件 | 说明 |
|------|------|
| `usb_segments_export.json` | 117 段 USB 相关走线原始导出 |
| `usb_trace_analysis.json` | 合并路径与阻抗估算 |
| `scripts/analyze_usb_traces.py` | 分析脚本（可重复运行） |
| `scripts/gen_usb_report_md.py` | 生成本报告第 7 节 USB 明细 |

- 工具：嘉立创EDA + Run API Gateway + easyeda-api Bridge  
- 操作：**仅读取**，未调用写入 API  
- 局限：U 形板框、部分 DRC 警告无 API 明细；敷铜可能未在 Fill/Region 接口体现  

---

## 11. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-06 | v1：初版检查报告（四层假设） |
| 2026-06-06 | **v2**：改为 **六层叠层**；补充 USB 三种线宽逐段/合并路径与阻抗估算 |

---

*阻抗估算基于 1.6 mm、εr≈4.2 的工程模型，**不能替代嘉立创阻抗神器与实测**。*

*原始数据：`usb_segments_export.json`（117 段）。*
"""


def main():
    usb = USB.read_text(encoding="utf-8")
    # gen script uses ## for top of USB section; renumber to fit under section 7
    usb = usb.replace("## USB 走线明细", "## 7. USB 走线明细", 1)
    body = HEADER + usb + FOOTER
    OUT.write_text(body, encoding="utf-8")
    print("written", OUT.stat().st_size)


if __name__ == "__main__":
    main()
