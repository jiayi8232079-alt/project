# Superpowers Skills 参考手册（精简版）

> superpowers v5.1.0 · 完整原文安装路径：`~/.claude/plugins/cache/claude-plugins-official/superpowers/5.1.0/`  
> 下列每条 skill 为原文摘要，**单条约 500 字以内**，便于检索；细节以仓库内 `SKILL.md` 为准。

---

## brainstorming

**功能：** 在写代码前把需求收敛成可评审的设计与规格，用对话澄清意图与约束。  
**调用场景：** 任何「加功能 / 改行为 / 做组件」类创意工作（文档强调再小的改动也要走流程，避免未检验假设）。  
**使用方式：** 先了解项目上下文；一次只问一个问题；给出 2–3 种方案与取舍；分节讲清架构、数据流、错误与测试并逐节确认；**在用户明确批准设计前禁止**调用实现类 skill、禁止写代码或脚手架；复杂可视话题可单独消息征求是否用 Visual Companion；将定稿写入 `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`（用户另有约定从其约定）；自检 spec 后请用户读文件确认；**下一步唯一应调用** `writing-plans`，不要直接跳到其它实现 skill。

---

## dispatching-parallel-agents

**功能：** 把彼此独立的问题域交给多个子代理并行处理，各自只拿你构造好的最小上下文，避免串行浪费与上下文污染。  
**调用场景：** 多个失败/调查互不依赖（不同测试文件、子系统、缺陷），或多项工作无共享状态、无固定先后顺序。  
**使用方式：** 按「一个独立域 → 一个 agent」拆分；为每个 agent 写独立、自包含的指令（目标、边界、验收、禁止事项），**不要**把你的整段会话历史原样塞过去；并行派出；回收结果后由你整合；若任务有依赖或需同一上下文连续决策，则不要用本模式。

---

## executing-plans

**功能：** 在**单独会话/批处理**语境下，按已写好的实现计划逐步执行任务并在阻塞时停下来。  
**调用场景：** 手头有书面计划（通常来自 `writing-plans`），且适合「加载计划 → 逐条做 → 验证」而非本会话内频繁派子代理。  
**使用方式：** 开场声明使用本 skill；**第 1 步**通读计划，有疑问先与用户对齐；**第 2 步**用 TodoWrite 逐 task：`in_progress` → 严格按计划小步执行并跑计划中写的验证 → `completed`；遇阻、计划缺口、反复验证失败则停止并求助，不要猜；全部完成后声明并**必须**调用 `finishing-a-development-branch`。与 `using-git-worktrees`、`writing-plans` 衔接；若平台支持子代理且任务独立，优先 `subagent-driven-development`。

---

## finishing-a-development-branch

**功能：** 在实现与测试已就绪时，用固定菜单驱动「合并 / 提 PR / 保留 / 丢弃」，并正确处理 worktree 清理。  
**调用场景：** 功能做完、测试应已通过，需要决定如何合入主线或交付。  
**使用方式：** 先跑完整测试套件，**失败则停止**，不进入收尾菜单；检测是否在普通仓库、命名分支 worktree 或 detached HEAD（后两者菜单选项数不同）；弄清 base 分支；**普通/命名分支**给出 4 选：本地合并、推送并开 PR、保留分支、丢弃；**detached** 给 3 选（无本地合并）；执行用户选择时注意：仅选项 1 与 4 在适当时清理 superpowers 管理的 worktree；合并须先成功再删分支；PR 路径不要提前删 worktree；丢弃需用户键入确认；`git worktree remove` 前先到主仓库根目录再操作。

---

## receiving-code-review

**功能：** 把 Code Review 当成技术核实流程：先读懂、再对照代码验证、再决定采纳或有理有据地反驳，避免社交化附和。  
**调用场景：** 收到他人 CR 意见准备改代码前；尤其意见模糊、与代码库现实不符或可能过度设计时。  
**使用方式：** 按「读 → 用自己的话理解/提问 → 对仓库事实做验证 → 判断是否适合本项目 → 技术化回复或 pushback → 逐项实现且每项带测试」；禁止未核实就说「太对了」或马上开改；**有任何一条不清楚就停**，先问清再动刀；区分人类伙伴与外部评审的语境；对「专业感」堆砌要保持 YAGNI；可接受的 pushback 需基于行为、性能、边界等你验证过的事实。

---

## requesting-code-review

**功能：** 在合适节点主动发起审查，给出审查者高效处理所需的上下文与检查清单。  
**调用场景：** 较大功能完成、架构或行为有非显而易见之处、准备合并/发布前、或你希望尽早发现盲点时。  
**使用方式：** 说明变更动机与范围；用要点列出关键文件与行为变化；给出**可执行的验证步骤**（命令、场景）；标明已知风险或取舍；请求对方关注具体方面（正确性、性能、API、测试等）；与 TDD、verification、finishing 等流程衔接，避免「扔 diff 不问问题」。

---

## subagent-driven-development

**功能：** 在**同一会话**内按实现计划推进：每任务派**全新**实现子代理，任务结束后做**两阶段评审**（先 spec 合规，再代码质量），必要时循环直到通过。  
**调用场景：** 已有计划、任务大多可独立、希望在本会话连续跑完且平台支持子代理。  
**使用方式：** 一次性读计划并抽出各 task 全文与上下文建 Todo；**每个 task**：派实现代理（只给该 task 文本，不让其自己读整份计划）→ 处理 DONE / NEEDS_CONTEXT / BLOCKED / DONE_WITH_CONCERNS → spec 评审 → 代码质量评审 → 全过再标 task 完成；**禁止**并行多个实现代理、禁止跳过任一评审、禁止在 spec 未通过前进入代码质量评审；模型按机械/集成/架构任务分级选用；全部 task 后再派总览评审并接 `finishing-a-development-branch`。子代理实现侧应遵循 `test-driven-development`。

---

## systematic-debugging

**功能：** 强制「先根因、后补丁」：用可复现证据与假设检验代替试错式乱改。  
**调用场景：** 任意 bug、测试红、构建失败、异常行为、性能问题；越紧急越要遵守，避免「先改一行试试」。  
**使用方式：** **四阶段**：① 根因调查——细读报错与栈、稳定复现、看近期变更、多层系统则在边界加诊断再判定点；② 模式对比——找同类可运行代码、完整读参考实现、列出差异；③ 假设与最小验证——一次只改一个变量验证假设；④ 实现——先写**最小失败用例/测试**再修根因（配合 TDD），一次只做一个修复，禁止顺手大重构；若已连续多轮修复仍摇摆，停下来讨论架构是否根本不合理。详见 skill 目录内 `root-cause-tracing.md` 等补充材料。

---

## test-driven-development

**功能：** 红-绿-重构：先写失败测试，看到「对的失败」，再写最少实现通过，再重构保持绿。  
**调用场景：** 新功能、修 bug、改行为、重构；用户/项目明确豁免的极少数情况需先征得伙伴同意。  
**使用方式：** 铁律：**没有先失败的测试就不写生产代码**；若已写反了顺序须删掉实现重来；RED 阶段测试要测真实行为、命名清晰、单测一事；**必须**运行并确认失败原因符合预期；GREEN 只写满足当前测试的最简代码，禁止夹带 YAGNI 功能；**必须**再跑全绿；REFACTOR 仅整理结构不加行为；抵制「先写代码再补测试」「测试一下就绿所以不用红」等借口。与 systematic-debugging、verification-before-completion 配合。

---

## using-git-worktrees

**功能：** 为功能开发或执行计划准备**隔离工作区**：优先用平台原生能力，否则退回 `git worktree`。  
**调用场景：** 开始需要与当前目录隔离的特性分支工作，或执行 `executing-plans` / `subagent-driven-development` 前。  
**使用方式：** **Step0** 用 `GIT_DIR` vs `GIT_COMMON` 判断是否已在 worktree，并排除 submodule 误判；已隔离则不再建第二层；若需新建且用户未表态，先征求同意；**Step1a** 有 `EnterWorktree` 类等原生工具则**禁止**手搓 `git worktree add`；**Step1b** 无原生工具时再按优先级选 `.worktrees/`、`worktrees/`、`~/.config/superpowers/worktrees/<项目>/` 等路径，项目内目录须 `git check-ignore` 确认已忽略；创建后做依赖安装与**基线测试**，失败则报告并询问是否继续。

---

## using-superpowers

**功能：** 约定「何时、如何」加载 skill：在回应用户**之前**先判断是否应调用 Skill 工具，并说明优先级与常见自我合理化陷阱。  
**调用场景：** 对话开始或任何任务中途——只要存在 ≥1% 可能适用某 skill 就要先加载核对（子代理若被派来执行单一任务可跳过本 skill，以 skill 内 SUBAGENT-STOP 为准）。  
**使用方式：** 用户文档（CLAUDE.md 等）优先于 skill；Claude Code 用 `Skill` 工具加载正文，**不要用 Read 偷看 skill 文件**；其它平台按各自 `skill`/`activate_skill` 等价物；流程上先想是否需 brainstorming/debugging 等**过程类** skill，再选实现类；有 checklist 的 skill 要拆成 Todo；宣称「太简单不用 skill」通常视为 red flag。多 skill 时：先过程、后实现。

---

## verification-before-completion

**功能：** 禁止「口头完成」：任何成功/满意/测过的断言必须建立在**本轮刚跑过**的验证命令输出之上。  
**调用场景：** 即将说「完成」「修好了」「测试过了」、提交、开 PR、切下一任务、或转述子代理「已成功」之前。  
**使用方式：** 铁律：**没有新鲜证据就不能下成功结论**；先明确哪条命令能证明你的陈述 → 完整执行 → 读退出码与输出 → 再说话；「应该过了」「上次跑过」「代理说 OK」都不算；区分测试绿、lint、构建成功等不同主张；回归测试要能做 red-green 论证；需求完成度应用清单对 spec 逐项核对。适用于同义改写与暗示成功的措辞。

---

## writing-plans

**功能：** 把已批准的 spec 变成**可交给他人或代理逐步执行**的实现计划：路径明确、步骤可勾选、含测试与命令与期望输出。  
**调用场景：** 多步骤需求已清晰，**在写实现代码之前**（通常接在 brainstorming 之后）。  
**使用方式：** 声明使用本 skill；默认保存到 `docs/superpowers/plans/YYYY-MM-DD-<feature>.md`；先梳理将创建/修改的文件职责再拆 task；每个 task 拆成 2–5 分钟粒度（写失败测试 → 跑红 → 最简实现 → 跑绿 → 提交）；计划头须含目标、架构简述、栈与「执行方须用 subagent-driven-development 或 executing-plans」的提示；**禁止** TBD、空泛「加错误处理」、不附代码的「写测试」等占位；写完后自检覆盖与一致性，再让用户选执行路径并切换到对应 skill。

---

## writing-skills

**功能：** 用 **TDD 做 SKILL 文档**：先让子代理在压力场景下暴露违规（基线），再写 `SKILL.md` 收紧规则，循环至稳定遵守。  
**调用场景：** 新建/大改 skill，或发布前验证代理能否按文档执行。  
**使用方式：** 先掌握 `test-driven-development`；skill = 可复用技法/模式/参考，非一次性战报；安装路径随平台（如 `~/.claude/skills`）；`SKILL.md` 须 YAML `name` 与 `description`（description 只写**触发条件**，短、第三人称、`Use when...`）；正文写步骤与反模式，用交叉引用代替 `@` 预加载；长参考拆附件；**无基线失败场景则 skill 未完成**。init/package 脚本与 Anthropic 最佳实践见插件目录原文。

---

*文档版本：精简汇编 · 与原 `SKILL.md` 不一致时以插件内源文件为准。*
