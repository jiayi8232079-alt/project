---
description: 新增/修改代码注释时，关键说明须用中文
alwaysApply: true
---

# 注释语言（全局）

- 由助手**新增或实质性修改**的代码注释（含行内 `//`、`/* */`、块说明），解释**意图、执行顺序、条件分支、资源生命周期、线程安全、已知限制或风险**的句子**须使用中文**。
- **可保留英文**的部分：符号名、类型名、文件名、配置宏、`tal_*` / `tuya_*` 等 API 字面、协议与芯片手册中的专有名词；必要时可在中文后括注英文术语。
- 若某文件已有统一的英文 Doxygen 文档风格，新增 `@brief`/`@param` 可与该文件保持一致；**行内注释与步骤说明仍以中文为主**。
